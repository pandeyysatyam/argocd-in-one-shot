apiVersion: v1
kind: Service
metadata:
  name: online-shop-service
  namespace: default
spec:
  selector:
    app: online-shop
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
  type: ClusterIP
